var firstNumber = 5;
var secondNumber = 10;


if (firstNumber > secondNumber) {
    console.log(`The Number ${firstNumber} is bigger than the number ${secondNumber}.`);
} else {
    console.log(`The Number ${firstNumber} is smaller than the number ${secondNumber}.`);
}