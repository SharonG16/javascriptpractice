var text = "game of thrones";
var firstChar = text.charAt(0);
var secondChar = text.charAt(5);
var thirdChar = text.charAt(8);
var res = firstChar.toUpperCase().concat(secondChar.toUpperCase(),thirdChar.toUpperCase());


console.log(`The final result is: ${res}`);